{
  "name": "bot-verifier-claim-relayer",
  "private": true,
  "type": "module",
  "description": "Base Sepolia claim-flow relayer. Fixture by default. Live submit only on chain 84532 when Spencer unlocks it.",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test test/*.test.mjs",
    "readonly": "node readonlyEscrow.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@noble/hashes": "^1.8.0",
    "viem": "^2.56.9"
  }
}
